[project]
name = "parallelism"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "hungryengineer", email = "noemail@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "torch>=2.14.0",
]

[project.scripts]
parallelism = "parallelism:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
